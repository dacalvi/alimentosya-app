import React from 'react';
import { Text } from 'react-native';

export default AYGroupTitle = (props) => {
    
    return (
        <Text style={{ fontWeight: 'bold', fontSize: 24}}>{props.title}</Text>                   
    )};